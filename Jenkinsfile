pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'spineless David'
                sh 'python3 --version'
                sh 'python3 hello-world.py'
            }
        }
    }
}
